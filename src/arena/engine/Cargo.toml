[package]
name = "arena-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.10.1"
rand_pcg = "0.10.2"
rapier2d = { version = "0.33.0", features = ["enhanced-determinism"] }
serde = { version = "1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
