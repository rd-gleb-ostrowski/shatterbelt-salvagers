/**
 * Admin entry point — stub for issue 08.
 *
 * The full Admin app (bot/team management, match control, ladder, replays,
 * diagnostics) is built in issue 08 on top of this scaffold.
 */

const root = document.createElement("p");
root.textContent = "Admin — not yet implemented (issue 08).";
document.body.appendChild(root);
